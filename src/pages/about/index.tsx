import React, { useEffect } from 'react';

const About: React.FC = () => {
  // const [data, setData] = useState<any>({});
  useEffect(() => {
    // console.log('页面加载1111');
  }, []);
  return <div>About</div>;
};

export default About;
